// ErrorContextKey Enum (simplified for MVI)
enum ErrorContextKey {
  componentName,
  operation,
  originalException,
  sourceIdentifier,
  dataFormat,
  mediaType,
  resourceLocation, 
  importSourceType, 
  ownerID,
  // ... other context keys
}