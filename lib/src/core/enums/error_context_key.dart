// ErrorContextKey Enum (simplified for MVI)
enum ErrorContextKey {
  componentName,
  operation,
  originalException,
  sourceIdentifier,
  dataFormat,
  mediaType,
  resourceLocation,
  // ... other context keys
}